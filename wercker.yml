box: node:latest
build:
  steps:
    - script:
        name: coffee-script/csfn install
        code: |
          npm install -g coffee-script
          npm install -g csfn
    - script:
        name: compile
        code: |
          csfn gennai.coffee > gennai.template

